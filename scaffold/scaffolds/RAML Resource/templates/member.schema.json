{
    "id": "http://types/{{answers.name}}",
    "type": "object",
    "$schema": "http://json-schema.org/draft-04/schema",
    "required": ["{{camelCase answers.name}}Id"],
    "additionalProperties": false,
    "properties": {
        "{{camelCase answers.name}}Id": {
            "description": "The identifier of the object",
            "type": "string"
        }
    }
}
